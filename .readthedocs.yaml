version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.8"

python:
  install:
    - method: pip
      path: .
    - method: pip
      path: requirements.txt
    - requirements:
        - sphinx>=3.0
        - sphinx-rtd-theme

sphinx:
  configuration: docs/conf.py
